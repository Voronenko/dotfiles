name: tdd
root: ~/

pre_window: |
#  echo "import environment, if any"

windows:
  - project_editor:
      layout: matrix
      panes:
        - echo pane 1
        - echo pane 2
        - echo pane 3
        - echo pane 4
  - project_server:
    - echo execute backend serverm if any
  - project_logs:
    - echo execute logs, if any
